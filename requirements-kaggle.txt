# Kaggle-specific requirements
# Usage in notebook cell: !pip install -r requirements-kaggle.txt
#
# NOTE: torch is pre-installed on Kaggle — do NOT reinstall it,
#       it will break the GPU drivers. Only add what Kaggle doesn't have.

transformers>=4.43.0
trl>=0.9.0
peft>=0.12.0
datasets>=2.20.0
accelerate>=0.31.0
bitsandbytes>=0.43.0
sentencepiece>=0.2.0
pyyaml>=6.0.1
